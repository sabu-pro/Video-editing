{
  "name": "cutline-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first, multitrack video editor for the browser",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/app.js && node --check src/engine.js && node --check src/core.js && node --check src/webm.js && node --check src/audio-effects.js && node --check src/noise-removal.js && node --check src/noise-worklet.js && node --check src/effects-catalog.js",
    "test:browser": "playwright test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
